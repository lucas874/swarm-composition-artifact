sudo docker build -t ecoop25_artifact .
sudo docker run -it ecoop25_artifact
sudo docker cp 035e711c2bcf:/ecoop25_artifact/process_results/results_short_run/out.pdf out120.pdf
sudo docker save -o ecoop25-artifact-image.tar ecoop25_artifact
docker save myimage:latest | gzip > myimage_latest.tar.gz
sudo docker load -i ecoop25-artifact-image.tar
sudo docker cp <container id>:/ecoop25_artifact/process_results/results_short_run results_short_


/usr/bin/supervisord -c /etc/supervisor/supervisord.conf
sudo docker ps -q -f ancestor=ecoop25_artifact:latest
sudo docker cp $(sudo docker ps --filter "ancestor=ecoop25_artifact:latest" --format "{{.ID}}"):/ecoop25_artifact/process_results/results_short_run .
sudo docker cp $(sudo docker ps --filter "ancestor=ecoop25_artifact" --format "{{.ID}}"):/ecoop25_artifact/process_results/results_short_run .


The reviewing template is from ecoop24 and links to ecoop24 submission template..

Send stdout and stderr to out_criterion_short.txt and run process in the background
cargo criterion --offline --output-format quiet --plotting-backend disabled --bench composition_benchmark_short &>out_criterion_short.txt&

sudo docker system prune -a
sudo docker system prune --volumes
sudo docker cp $(sudo docker ps --filter "ancestor=ecoop25_artifact" --format "{{.ID}}"):/ecoop25_artifact/results_short_run .

--rm    Automatically remove the container when it exits (incompatible with -d). The default is false. see manpages
--rm true|false
          Automatically remove the container when it exits. The default is false.
          --rm flag can work together with -d, and auto-removal will be done on daemon side. Note that it's incompatible with any restart policy other than none.
sudo docker run -it --rm ecoop25_artifact

to run creating a bind mount run e.g.
sudo docker run -it --rm -v $(pwd)/machines:/ecoop25_artifact/machines ecoop25_artifact

mounting multiple things:
sudo docker run -it -v $(pwd)/warehouse-demo:/ecoop25_artifact/warehouse-demo -v $(pwd)/results_short_run:/ecoop25_artifact/results_short_run ecoop25_artifact

RUST_LOG=debug cargo test
RUST_LOG=debug cargo test


sudo docker run -it -v $(pwd)/demos:/ecoop25_artifact/demos -v $(pwd)/results_short_run:/ecoop25_artifact/results_short_run ecoop25_artifact
sudo docker run -it -v $(pwd)/demos:/ecoop25_artifact/demos -v $(pwd)/results:/ecoop25_artifact/results $(pwd)/demos:/ecoop25_artifact/logs ecoop25_artifact
